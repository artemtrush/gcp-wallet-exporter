{
    "bankName": "{{BANK_NAME}}",
    "maxMonthsToExport": "{{MAX_MONTHS_TO_EXPORT}}",
    "timeZone": "{{TIME_ZONE}}",

    "importVia": "{{IMPORT_VIA}}",
    "importFileFormat": "{{IMPORT_FILE_FORMAT}}",

    "cloudStorage": {
        "bucketName": "{{CLOUD_STORAGE_BUCKET_NAME}}"
    },

    "proxy": {
        "host": "{{PROXY_HOST}}",
        "port": "{{PROXY_PORT}}"
    },

    "wallet": {
        "importUrl": "{{WALLET_IMPORT_URL}}",
        "userId": "{{WALLET_USER_ID}}"
    },

    "mail": {
        "from": "{{MAIL_FROM}}",
        "to": "{{MAIL_TO}}",
        "smtp": {
            "host": "{{SMTP_HOST}}",
            "port": "{{SMTP_PORT}}",
            "username": "{{SMTP_USERNAME}}",
            "password": "{{SMTP_PASSWORD}}"
        }
    }
}